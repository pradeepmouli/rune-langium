namespace "test.model"
version "test"

type Animal:
    name string (1..1)

type Dog extends Animal:
    breed string (1..1)

enum Color:
    Red
    Green

enum DogColor extends Color:
    Brown
    Black
