.claude/settings.local.json
.claude/worktrees/
.claire/
.cursor/
.kiro/
.windsurf/

# Infigraph local index/cache (regenerated per-machine; docs.kuzu, embeddings,
# graph, session logs, and watch state all churn on every reindex)
.infigraph/
node_modules/
dist/
.wrangler/
build/
coverage/
.coverage/
*.log
.DS_Store
.superpowers/
.env
.env.local
.env.*.local
.nyc_output/
.pnpm-debug.log*
*.tsbuildinfo

# Disposable Xtext source clone for grammar conversion
.rune-dsl/

# Vendored fixture snapshots (CDM corpus, Rune DSL built-in types)
# Fetched by scripts/update-fixtures.sh — not published to npm
.resources/

# Playwright test artifacts (root-level stale runs)
test/e2e/__snapshots__/
test/e2e/__screenshots__/
test-results/
playwright-report/
.playwright-mcp/
*.png
docs/html/

# Generated API docs (typedoc)
docs/api/

# Generated llms.txt / llms-full.txt — emitted by typedoc-plugin-to-skills
# at repo root on every docs build.
/public/

# CF Pages deploy staging — moved WIP from the pre-CF-Pages unified build
.attic/
apps/docs/.vitepress/.temp/
apps/docs/.vitepress/dist/
apps/docs/.vitepress/cache/

# CF Pages discovery surfaces (spec 019). Generated by
# apps/docs/scripts/build-combined.mjs into the repo root because CF Pages
# scans `<Root Directory>/functions/` and reads `<Root Directory>/wrangler.toml`
# at deploy time. Both are deploy-only artifacts — the source of truth for
# functions is apps/studio/functions/.
/functions/
/wrangler.toml
reference-design/build-daikonic-mark.py
reference-design/daikonic-mark-geometric.svg
reference-design/daikonic-mark.svg
reference-design/uploads/Image 1.png.2026_06_04_12_57_53.0.svg
reference-design/ux-functionality-differential-2026-06-05.md
